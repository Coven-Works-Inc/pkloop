import React from 'react'
import HeaderFooter from '../header_footer'

const Parcel = () => {
    return (
        <HeaderFooter>
            <p className="empty-page">Parcel</p>
        </HeaderFooter>
    )
}

export default Parcel;